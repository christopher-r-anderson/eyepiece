import { Checkbox as RacCheckbox } from 'react-aria-components'

// unstyled adapter export: consumers style per surface for now; a proper
// recipe belongs to the component pass that designs checkboxes broadly
export const Checkbox = RacCheckbox
