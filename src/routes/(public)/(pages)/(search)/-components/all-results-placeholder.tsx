// Interim all-scope state; replaced by the sectioned view in #92 stage 2.
export function AllResultsPlaceholder() {
  return (
    <p css={{ textAlign: 'center', margin: 'var(--space-7) 0' }}>
      Choose a library above to see its results.
    </p>
  )
}
