node_modules
.DS_Store
dist
dist-ssr

# Panda CSS codegen output (regenerated by the prepare script)
/styled-system/

# ITF-FFL fonts are fetched at build time, not redistributed (scripts/fetch-fonts.mjs)
/public/fonts/zodiak-*.woff2
/public/fonts/switzer-*.woff2
*.local
count.txt
.env
.env.test
.nitro
.tanstack
.wrangler
.output
.vinxi
todos.json

# Local Netlify folder
.netlify
# Ignored because we don't use edge functions
deno.lock

# Playwright
/test-results/
/playwright-report/
/e2e/.fixture-misses.log
/blob-report/
/playwright/.cache/
/playwright/.auth/

# Provider sampling output (scripts/sample-provider-text.ts), regenerated on demand
/provider-samples/

# Lighthouse/axe audit reports (scripts/lighthouse-audit.ts, scripts/axe-audit.ts)
/audit-reports/
.claude/settings.local.json
